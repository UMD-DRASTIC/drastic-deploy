import ldap
from django_auth_ldap.config import LDAPSearch, GroupOfNamesType

# Baseline configuration.
AUTH_LDAP_SERVER_URI = "{{ LDAP_SERVER_URI }}" # ldap://ldap.example.com

AUTH_LDAP_BIND_DN = "{{ LDAP_BIND_DN }}" # "cn=django-agent,dc=example,dc=com"
AUTH_LDAP_BIND_PASSWORD = "{{ LDAP_BIND_PASSWORD }}" # "phlebotinum"
AUTH_LDAP_USER_DN_TEMPLATE = "{{ LDAP_USER_DN_TEMPLATE }}" # "uid=%(user)s,ou=users,dc=example,dc=com"

# Populate the Django user from the LDAP directory.
AUTH_LDAP_USER_ATTR_MAP = {
    "first_name": "givenName",
    "last_name": "sn",
    "email": "mail"
}

# Keep ModelBackend around for per-user permissions and maybe a local
# superuser.
AUTHENTICATION_BACKENDS = (
    'django_auth_ldap.backend.LDAPBackend',
    'django.contrib.auth.backends.ModelBackend',
)
